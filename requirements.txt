# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# PDF processing
pymupdf>=1.24
pymupdf4llm>=0.0.17

# Word (.docx) export for extracted text. Fallback renderer when pandoc is not
# installed; pandoc, if present, is preferred for Markdown fidelity (headings,
# tables, lists).
python-docx>=1.1

# CMap repair (OpenPecha) — the core tool this app wraps
pdf-cmap-fix @ git+https://github.com/OpenPecha/pdf-cmap-fix.git@007ef5b8744d4fed8f0c5ddd2eb445b0f8a02600
